#include "BaseBabelEnter.h"

#include <pdzBase/Diagnostic.h>

BaseBabelEnter::BaseBabelEnter(const MetaType::Key metaTypeKey,
                               const QString & string)
{
    USE(metaTypeKey); USE(string); TODO();
}
